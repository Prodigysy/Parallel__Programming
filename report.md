# Отчет по лабораторной работе №3

## Задание
Разработать программу на языке C/C++ для параллельного перемножения матриц с использованием MPI.
Входные данные — матрицы, сохранённые в текстовых файлах.
Результаты работы — файл с произведением, замер времени исполнения и размеры задачи.
Обязательна проверка корректности с использованием внешних инструментов (например, Python с NumPy).

## Реализация
Проект реализован на C++ с применением библиотеки MPI (в частности, MPICH). Основу составляет класс Matrix, содержащий методы для генерации, чтения, записи и параллельного перемножения матриц.

## Этапы выполнения алгоритма:

1. На процессе с рангом 0 создаются две случайные матрицы заданного размера.
2. Строки первой матрицы равномерно распределяются между всеми доступными процессами.
3. Вторая матрица передаётся полностью каждому процессу через широковещательную передачу.
4. Каждый процесс выполняет умножение выделенной ему части первой матрицы на полную вторую матрицу.
5. Все локально вычисленные фрагменты результирующей матрицы пересылаются обратно процессу 0 для сборки полной матрицы.
6. Результат сохраняется в файл (только для одного из выбранных размеров матриц).
7. Для каждой размерности операция повторяется 10 раз, после чего вычисляется среднее время выполнения.


### Верификация
Для верификации используется Python-скрипт, в котором матрицы из файлов загружаются и перемножаются с помощью numpy.dot(). Результат сравнивается с тем, что выдала C++ программа.

## Результаты
Измерено среднее время выполнения на 10 прогонах при использовании 4 и 12 MPI-процессов:

| Размер | Последовательный запуск | MPI (4 процесса) | MPI (12 процессов) |
| ------ | ----------------------- | ---------------- | ------------------ |
| 100    | 0.0109 с                | 0.00129 с        | 0.00145 с          |
| 200    | 0.1137 с                | 0.01058 с        | 0.00892 с          |
| 300    | 0.3062 с                | 0.03141 с        | 0.02463 с          |
| 400    | 0.6769 с                | 0.07912 с        | 0.04622 с          |
| 500    | 1.3351 с                | 0.14359 с        | 0.08648 с          |
| 600    | 2.3712 с                | 0.23361 с        | 0.14172 с          |
| 700    | 3.7709 с                | 0.37738 с        | 0.22657 с          |
| 800    | 5.6097 с                | 0.58146 с        | 0.34189 с          |
| 900    | 8.1985 с                | 0.83921 с        | 0.49781 с          |
| 1000   | 10.8307 с               | 1.13503 с        | 0.66337 с          |
      


![График](https://github.com/Prodigysy/Parallel__Programming/blob/lab_3/results.png)

### Работа на суперкомпьютере
![График](https://github.com/Prodigysy/Parallel__Programming/blob/lab_3/Korolev4-12.png)
На суперкомпьютере алогритм показывает более стабильную работу (происходит практически трехкратное улучшение при увеличении кол-ва потоков в три раза).

## Вывод
Использование MPI позволило добиться значительного ускорения при выполнении матричного перемножения. Особенно эффективно это проявляется на крупных задачах и при большом числе потоков.
В отличие от OpenMP, MPI обеспечивает лучшее масштабирование при работе в распределенной среде, хотя и требует дополнительных затрат на передачу данных между узлами.
В целом, MPI подтвердил свою надежность и эффективность в задачах подобного класса.
